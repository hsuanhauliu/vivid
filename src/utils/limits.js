// Reasonable input-length ceilings for user-entered text fields, shared across
// the app so name/description/tag limits stay consistent between the detail
// pane, collections, playlists, and folders.

export const NAME_MAX_LEN = 150; // media item display name
export const DESCRIPTION_MAX_LEN = 1000; // media item / collection description
export const TAG_MAX_LEN = 30; // a single tag
export const MAX_TAGS = 10; // custom tags per media item
export const COLLECTION_NAME_MAX_LEN = 60; // album / playlist / folder name
export const AUDIO_FIELD_MAX_LEN = 100; // track title / artist / album
